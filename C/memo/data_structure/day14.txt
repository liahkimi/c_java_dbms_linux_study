스택(Stack)
	- 데이터를 제한적으로 접그할 수 있는 구조
	- 가장 나중에 쌓은 데이터를 가장 먼저 빼낼 수 있는 구조
	- LIFO(Last Input First Out)
	a(	 ){	b( ){ 		c(	);	}	}


큐(Queue)
	- 데이터를 제한적으로 접그할 수 있는 구조
	- 가장 먼저 쌓은 데이터를 가장 먼저 뺴낼 수 있는 구조
	- 출구와 입구가 다른 구조라고 생각하면 쉬움
	- FIFO(First Input First Out)
	- 프로그램 실행 방식이 큐 방식(윈도우 운영체제의 경우)